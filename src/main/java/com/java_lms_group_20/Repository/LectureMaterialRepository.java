package com.java_lms_group_20.Repository;

import com.java_lms_group_20.Model.LectureMaterial;
import java.util.ArrayList;
import java.util.List;

public class LectureMaterialRepository {

    /*
    private final List<LectureMaterial> lectureMaterials = new ArrayList<>();

    // Add lecture material
    public void add(LectureMaterial material) {
        lectureMaterials.add(material);
    }

    // Get all lecture materials
    public List<LectureMaterial> findAll() {
        return lectureMaterials;
    }

    // Find by ID
    public LectureMaterial findById(int id) {
        for (LectureMaterial material : lectureMaterials) {
            if (material.getId() == id) {
                return material;
            }
        }
        return null;
    }

    // Delete by ID
    public boolean delete(int id) {
        return lectureMaterials.removeIf(material -> material.getId() == id);
    }
    */
}
